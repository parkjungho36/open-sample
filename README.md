# Project Website

Internal website project.

## Google sign-in

Create a Google OAuth web client and add its client ID to the
`google-oauth-client-id` meta tag in `index.html`. Register the production
GitHub Pages URL and local preview URL as authorized JavaScript origins.
